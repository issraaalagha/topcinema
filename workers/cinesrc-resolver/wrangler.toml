name = "cinesrc-resolver"
main = "src/index.js"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# Cloudflare Browser Rendering (Workers Paid plan required)
browser = { binding = "BROWSER" }
